## Development

- **Dev**: `pnpm dev:index-app` / `pnpm dev:symbol-picker` — each app runs independently; index-app shows UI only, links do not work
- **Preview**: `pnpm build` → `pnpm preview` — check links from index-app